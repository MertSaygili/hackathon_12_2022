import 'package:flutter/material.dart';

const Icon iconKey = Icon(Icons.key);
const Icon iconPerson = Icon(Icons.person);
const Icon iconVisibltyOn = Icon(Icons.visibility);
const Icon iconVisibilityOff = Icon(Icons.visibility_off);
